package com.rahulghag.splittrip.core.ui.theme

import androidx.compose.ui.unit.dp

object Dimens {
    val spaceXS  = 4.dp   // icon padding, tight gaps
    val spaceS   = 8.dp   // between inline items
    val spaceM   = 12.dp  // row internal gap
    val spaceL   = 16.dp  // screen horizontal padding ← use this most
    val spaceXL  = 20.dp  // card internal padding
    val space2XL = 24.dp  // section gaps
    val space3XL = 32.dp  // between major sections
    val space4XL = 48.dp  // top screen padding
}
